spring:
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/jwt_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: qhdwnsgud1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update  # ?? ??? update, ????? validate ?? none ?? ??
    show-sql: true
